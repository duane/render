#ifndef __INCLUDE_MATH_UTILITY_H__
#define __INCLUDE_MATH_UTILITY_H__

static inline double max(double a, double b) {
    return a > b ? a : b;
}

static inline double min(double a, double b) {
    return a < b ? a : b;
}

#endif